services:
  - type: web
    name: ctf-challenge
    env: node
    buildCommand: npm install && npm run generate
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
